name: Build, Analyze, Scan
on:
  push:
    branches:
      - main

jobs:
  build-analyze-scan:
    name: Build
    runs-on: ubuntu-latest  # Ensures compatibility with GitHub-hosted runners
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Ensures correct analysis history

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Install Trivy (Fixed)
        run: |
          sudo apt-get update
          sudo apt-get install -y wget curl
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh
          sudo mv ./bin/trivy /usr/local/bin/  # Move to a directory in PATH
          echo "export PATH=$PATH:/usr/local/bin" >> ~/.bashrc  # Ensure PATH is updated
          source ~/.bashrc  # Reload shell configuration
          trivy --version  # Verify installation

      - name: Trivy File Scan
        run: trivy fs . > trivyfs.txt

      - name: Build and analyze with SonarQube
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}